import * as React from 'react';
import * as CSS from 'csstype';
import { InteractiveLink } from './InteractiveLink';
import { Code, P } from './UI';

const pageLinkContainerStyle: CSS.Properties = {
  display: 'block',
  margin: '1vh 0',
};

export const T13DControllerConversion: React.VFC = () => (
  <div>
    <P>This is T13DControllerConversion! it's EVEN... it's not as cool. I'm sorry. I know you got excited.</P>
  </div>
);
